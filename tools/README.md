# Tools

This directory stores vendored helper code and build-time tooling that should not be confused with editable rule sources.

## Layout

- `geoip/`: trimmed upstream geoip generator source kept for reference and future maintenance

Repository-specific metadata from the upstream project, such as its own CI files, is intentionally not kept here.
